OTD EVENT TIMER v2

The selected custom race uses the old single-race timer interface.
Use the horizontal tabs or Races panel to switch races.
All started races continue timing independently in the background.

Employee login is verified against the shared Supabase employee_access table.
The Bridge publishes saved employee_pins.docx changes automatically after the included Bridge support patch is installed.
